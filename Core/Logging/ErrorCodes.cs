namespace Vulpes.Perpendicularity.Core.Logging;

public static class ErrorCodes
{
    public static string INVALID_EMPTY_VALUE { get; } = $"[{nameof(INVALID_EMPTY_VALUE)}";
    public static string INVALID_VALUE { get; } = $"[{nameof(INVALID_VALUE)}";
}
